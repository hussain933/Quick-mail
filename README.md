# Quick Mail
Lightweight offline-first Android email sender with Gmail SMTP.  
Java UI + Rust engine. No cloud. End-to-end encryption of stored credentials.

## Build
1. Install Rust, Android NDK, cargo-ndk.
2. Run `rust-engine/build_android.sh` to build native libs.
3. Open project in Android Studio and build APK.
