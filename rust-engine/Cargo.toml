[package]
name = "quickmail_engine"
version = "0.1.0"
edition = "2021"

[lib]
name = "quickmail_engine"
crate-type = ["cdylib"]

[dependencies]
lettre = { version = "0.11", features = ["smtp-transport", "builder", "native-tls"] }
jni = "0.21"
log = "0.4"
android_logger = "0.13"
